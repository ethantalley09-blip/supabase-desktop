# Lynx — Handoff

Branch `feat/lynx-platform` · the TS→JS conversion + prior AI Advisor rounds are committed (`28e7fd0`) · **this session's §8 Survey work + a live-verification bug fix are uncommitted** · updated Jul 28, 2026

## Links

- **Live app** (Vercel, auto-deployed after commit): https://app.lynxcampaigns.com
- **GitHub**: https://github.com/ethantalley09-blip/supabase-desktop/tree/feat/lynx-platform
- **Supabase dashboard**: https://supabase.com/dashboard/project/ykrxgubcmrddzooauzzv

## Sign in (local dev)

| Email | Role | Password |
|---|---|---|
| carol@example.com | Owner — sees everything | password123 |
| finn@example.com | Canvasser | password123 |
| admin@lynx.app | SuperAdmin → /admin | password123 |

Re-seed after any `npm run db:reset` with `powershell -ExecutionPolicy Bypass -File scripts\seed-dev.ps1`.

---

# ⚡ Resume here

**This update: the §8 Survey `[Derived]`-tier remainder was built, and — for the first time all project — the app was actually verified live against a running local Supabase, which caught a real bug.** Owner asked to finish the loose ends left by the prior session's handoff and then "audit again to check for errors and fix them."

- **§8 Survey completion rate / abandonment / answer-choice skew, previously deferred pending a response-capture redesign — that redesign is now built.** New migration `0035_survey_responses.sql`: `campaign_scripts` gains an optional `choices text[]` column (null = open-ended question, defined = structured/multiple-choice — only a structured question can have a meaningful "skew"), and a new append-only `survey_responses` table (`visit_id` → `canvass_visits`, `script_id` → `campaign_scripts`, `answer`) records each question actually answered during one real door visit. New pure module `src/features/scripts/surveyAnalytics.js` (+ 12 tests): `computeSurveyCompletion` (of real CONTACTED visits, what fraction had every active question answered), `computeSurveyAbandonment` (of attempted-but-incomplete visits, the first unanswered question in real sort order — an honest per-question drop-off point, not just one aggregate rate), `computeAnswerChoiceSkew`/`mostSkewedQuestion` (min-5-answers floor, same sample-size discipline as the rest of the advisor). Three new advisor answers (`answerSurveyCompletion`, `answerSurveyAbandonment`, `answerAnswerChoiceSkew`) folded into the existing Advisor Insights card. Data-entry side: `useTurf.js`'s `logVisit` now returns the new row's id (previously fire-and-forget) so a new `useLogSurveyResponses` hook can attach responses to the specific visit that produced them; `BallotChase.jsx` gained a "Survey" column (only rendered when active structured/open questions exist) — a `SurveyCell` per voter with a select for a choices-defined question or free text otherwise, batched behind one "Save answers" button since a partial submit is real signal (an abandoned survey), not mid-typing noise. `AiCenterTab.jsx`'s survey-question composer gained an optional comma-separated "Choices" field.
- **Live verification (Docker + local Supabase, seeded, signed in as carol@example.com) — the first time this entire project's AI Advisor work was checked against a real running app, not just `npm test`/`npm run build`.** Caught one real bug: **the Owner role template never had `hr.view`/`hr.manage` for `campaign_committee` or `pac` org types** — only `nonprofit`/`party_committee` — a `0003_roles.sql` design predating the Staffing/Logistics feature that assumed HR/Payroll concepts didn't apply to lean campaign committees. Round 5 (Staffing/Logistics) built directly on `hr.view`/`hr.manage` believing Owner already had it everywhere ("already granted... a strong signal they were anticipated"), and its own resume instructions asserted "carol@example.com (Owner — has hr.manage)" — never true for the seeded `campaign_committee` org, and never caught because Docker wasn't running that whole session. **Result: the Staffing/Logistics section was completely invisible to Owner on the product's own primary org type.** Fixed in new migration `0036_hr_permission_campaign_pac.sql` — extends Owner's `campaign_committee`/`pac` template grants to match what `nonprofit`/`party_committee` Owners already had; deliberately did NOT add a dedicated HR role template for these org types, a bigger product decision than this bug fix calls for. Verified post-fix: signed in as Carol, added a real shift and a real hotel booking through the UI, confirmed both write successfully and the "Who is scheduled today?" / "What is our projected hotel cost?" advisor answers update to reflect them live. Also verified: all 7 project tabs load with zero console errors, adding a structured survey question (with choices) round-trips correctly through RLS and updates its advisor answer, and the new Survey column renders in `BallotChase.jsx` with the correct empty-state colspan.
- `npm run test` (470/470, +12 from this round) and `npm run build` both pass. Not yet committed — review and commit alongside the still-uncommitted §8/verification work above.

**Prior: post-conversion audit — full codebase swept for conversion errors — none found.** Owner asked to "audit everything, fix errors if any." Checked: (1) grepped every `.js`/`.jsx` file for leftover TS syntax (colon type annotations, `interface`/`type` declarations, `as`-assertions, `readonly`/`private`/`public`/`implements`/`declare`, generic `<T>` function/component syntax) — zero real hits, only false-positive English-word/ternary/comment matches; (2) checked for duplicate `.js`+`.jsx` pairs from a possible script bug — none; (3) checked for empty leftover files — none; (4) spot-checked the highest-risk patterns by hand: `forwardRef`-based UI components (`button.jsx` etc. — generics erased correctly), the 79-value `AiPurpose` union type in `useAiAssist.js` (a large type-only export — erased to nothing but the real `useAiAssist` function, exactly as expected), `client.js`'s `createClient(...)` call (generic argument correctly dropped). Re-ran `npm run build` (3520 modules, zero errors) and `npm run test` (458/458) fresh as the closing verification. **No errors found; nothing needed fixing.** `git diff --stat` against the `66d6630` checkpoint: 232 tracked files changed (net -32,069 lines, since JSX output is generally more compact than the TS source it replaced — comments/blank-line handling differences from `ts.transpileModule`, not lost logic; build+test passing confirms nothing functional was lost).

**Prior change: the ENTIRE codebase was converted from TypeScript to plain JavaScript/JSX, per an explicit owner directive.** Owner asked to "make sure all are in JSX format not TSX," clarified (via an explicit decision point, since `.tsx` already contains JSX — the ask meant stripping TypeScript entirely) to mean the whole codebase, not just this session's new files, after being shown the tradeoffs (loses `npm run typecheck`, changes tsconfig/vite config, no more compile-time type safety). Given nothing this session was committed, a git checkpoint commit (`66d6630`) was made first at the owner's explicit confirmation, so this is cleanly revertible if needed. Conversion method: the TypeScript compiler's own `ts.transpileModule`, run once via a throwaway Node script across all 170 non-test + 53 test `.ts`/`.tsx` files (`src/`, `supabase/functions/`, plus `vite.config.ts` and `api/census-geocode/[...path].ts`) — chosen over hand-editing because it's the same per-file type-erasure mechanism the dev server already used at runtime, so JSX/formatting came through nearly untouched, and because hand-converting 223 files manually would have been far slower and much more error-prone. Fixed up afterward: `index.html`'s `main.tsx` reference, `vite.config.js`'s test-setup path, `package.json` (`build` script dropped `tsc &&`, `typecheck` script removed), `tsconfig.json`/`tsconfig.node.json` deleted. Two files were pure type declarations with no runtime output and got deleted outright (`src/vite-env.d.ts`, `src/features/export/types.ts`); `src/lib/supabase/types.ts` had one real export (`Constants`, unused elsewhere) and survived as `types.js`. **Verified thoroughly, given the scale**: `npm run build` (3520 modules transformed, zero errors), `npm run test` (458/458, unchanged), and a live browser check (login page rendered correctly, zero console errors) — the first live-browser check that actually worked this entire session, since it doesn't require Supabase sign-in. **CLAUDE.md itself was updated** to stop documenting a stack/workflow that no longer exists: the header no longer says "TS", the `typecheck` step is removed from "verify before claiming done," the "regenerate types" workflow step is marked vestigial, and a new "Sharp edges already hit" entry documents the conversion in full for future sessions — most importantly, that there is now **no compile-time type safety anywhere in this app**, a real and permanent regression, not a style change. Not committed (this JSX conversion itself, i.e. the changes on top of the `66d6630` checkpoint) — review and commit when ready.

**Prior change: §20 Scenario planning, §21 Alerts, and §23 Rollup all extended — the last of the "easier" list before the bigger §8 Survey remainder.** §20 (`advisorInsights.ts`, +2 tests): `computeAverageDailyDoors` + `answerSkipADayImpact`, plus a second "Canvassing pace simulator" added to the UI (mirrors the existing fundraising one — doors-remaining goal + pace-change %, using `scenarioAdjustPace` already built). Most of §20's other phrasings (add/lose N canvassers, productivity ±X%, weather halves production) are just parameterizations of `scenarioAdjustPace`/`scenarioAddWorkers` already built in Round 3 — the two UI simulators let a user try any of them by changing the inputs, so they don't need one bespoke function each. §21 (+3 tests): `describeContactRateDropAlert` — a third alert, genuinely distinct from the existing two (which are about VOLUME — doors knocked, dollars raised); this one is about QUALITY (contact rate), which can drop for reasons volume alone wouldn't surface (bad turf, bad script, bad targeting). §23 (`projectRollup.ts`, +2 tests): `rankProjectsByFundraising`, a plain sort on data the rollup already fetches (no new query), surfaced as a "Strongest fundraising pace" line in the `ProjectAttentionRollup` widget. Two test bugs caught and fixed during this round — both in my own fixtures (a trailing-window boundary off-by-one, and a 10-generated-vs-7-in-window mismatch), not in the implementation; full test run confirmed clean after the fix. `npm run typecheck` / `npm run test` (458/458, up from 451, +7 new) / `npm run build` all pass. Not verified live. Not committed. **That closes out every "easier" section from the owner's stated order. Only §8 Survey's bigger remainder (completion rate, abandonment, answer-choice skew) is left — that one needs a structured response-capture redesign (recording every survey answer, not just the current question list), a different kind of change than everything else in this round. Owner has not yet given the go-ahead for that one.**

**Prior change: §12 Fundraising and §16 Social both filled out, continuing down the owner's stated order.** §12 (`advisorInsights.ts`, +8 tests): `answerTopFundraiser` (real $ by staff `recorder`), `answerPaymentMethodBreakdown` (the honest proxy for "source" this app has — no online/mail/event/social channel is tracked on a donation, only how it was paid, worded accordingly), `answerDonorGrowthVsAverageGift`, `answerDonorConcentration` (top-10%-of-donors share of total), `answerDonorRepeatShare` (repeat vs. first-time-in-this-dataset revenue split, explicitly caveated as a simplification since it can't see gifts outside the fetched window). §16 (`advisorInsights.ts`, +6 tests; `advisorData.ts` gained a `content` field on `AdvisorSocialPost` so posts can be quoted): `answerWeeklySocialReach`, `answerBestPerformingPost` (min-impressions floor so a lucky/unlucky single viewer can't win), `answerPostingFrequency` (deliberately factual-pace-only — this app sets no target cadence, so "too often" stays the reader's judgment call, never asserted). Skipped on purpose in both sections: anything needing cost/ROI data (no ad spend or acquisition cost tracked), per-channel donor conversion (no touch-sequence tracking), follower counts/media mentions/sentiment (not tracked anywhere), topic/format tagging (doesn't exist). `npm run typecheck` / `npm run test` (451/451, up from 434, +17 new) / `npm run build` all pass. Not verified live. Not committed. **Next up: §20 Scenario planning, §21 Alerts, §23 Rollup, then the bigger §8 Survey remainder.**

**Prior change: §7 Turf/geography filled out — 7 new advisor answers, continuing down the owner's stated order.** All new functions in `advisorInsights.ts` (+ 12 new tests): `answerBestContactRateTerritory`/`answerWorstContactRateTerritory` (wrap the existing but previously-unexposed `rankTerritoriesByContactRate`), `answerNextTerritoryToCanvass` (new — deliberately NOT filtered by `territoryDifficulty.ts`'s 3-attempt floor, since a totally fresh territory has no rate to protect and can still have the most remaining doors; reuses `turfBriefingMath.ts`'s own `remainingDoorsToday` so it never disagrees with the live heatmap), `answerTerritorySupportBreakdown` (new — real `persuadability_bucket` aggregated per turf), `answerWalkbookSizeOutliers` (new — real voter-count-per-territory vs. the real average, no fixed target exists so outlier = 50%+ off average), `answerUnattemptedDoors` (new — real active mapped voters with zero rows in `canvass_visits`), `answerRevisitCandidates` (reuses `revisitQueue.ts`'s existing repeated-no-answer detector directly). Skipped on purpose: items needing labor-hour tracking (not captured anywhere), weather/Census/turnout-history comparisons (tagged `[Integration]` in the source doc), and a "turf status" classifier (no defined "complete" threshold exists, would be inventing one). `npm run typecheck` / `npm run test` (434/434, up from 422, +12 new) / `npm run build` all pass. Not verified live. Not committed. **Next up: §12 Fundraising, §16 Social, §20 Scenario planning, §21 Alerts, §23 Rollup, then the bigger §8 Survey remainder.**

**Prior change: §5 Canvassing performance filled out — 10 new advisor answers, going one section at a time down the remaining "easier" list per owner direction.** Owner deferred §13 Compliance (agreed to revisit later, kept excluded), then asked to work through the buildable sections one at a time starting with Canvassing. All 10 new functions live in `advisorInsights.ts` (+ 14 new tests): `answerDoorsKnockedToday`/`answerTodaysContactRate`/`answerAvgDoorsPerCanvasserToday` (today-only, local-day boundaries matching the `canvasserFatigue.ts` pattern), `answerTopCanvasserThisWeek` (leaderboard filtered to trailing 7 days), `answerCanvasserMomentum` (new: week-scoped improving/declining trend, first-half-vs-second-half of the trailing week per canvasser — the week-scoped analog of `canvasserFatigue.ts`'s same-day version), `answerTeamContactRate` (new, genuinely cross-domain: `canvass_visits` has no team field, so this joins against the new `shifts` table by `profile_id + shift_date` to attribute visits to a team — only possible because of last round's Staffing work), `answerBestDayOfWeek`/`answerWeekendVsWeekday` (new day-of-week aggregation), `answerCoachingPairs` (pairs the strongest canvasser with the one with the most room to grow, framed as a shadow-shift suggestion — deliberately NOT "who needs a performance conversation" or "who's ready to lead," which the source doc itself tags `[Guarded]`/needs human judgment, left out on purpose), `answerTodayVsBaseline` (today's total vs. trailing-7-day average, with an honest note when a swing looks more like a staffing change than a pace change). `npm run typecheck` / `npm run test` (422/422, up from 408, +14 new) / `npm run build` all pass. Not verified live. Not committed. **Next up per the owner's stated order: §7 Turf/geography, then §12 Fundraising, §16 Social, §20 Scenario planning, §21 Alerts, §23 Rollup, then the bigger §8 Survey remainder.**

**Prior change: diagnosed exactly why the remaining question-bank sections were stuck, and fixed the one that was a real gap — the others aren't bugs.** Owner asked to "figure out the problem why the stuff that hasn't been completed aren't, fix it, and continue." Re-checked each against the source doc's own Now/Derived/Integration/Guarded tags rather than treating them as one bucket: §13 Compliance is a hard rule (CLAUDE.md invariant #6), not fixable by more code. §14-15 SMS/email tables are tagged `[Now]` in the doc but presuppose Lynx already sends messages — it structurally can't (no outbound provider wired up at all), so building "delivery rate" tracking with nothing to ever populate it would be empty scaffolding, genuinely blocked on the owner picking a provider. §17 Events and §18 Petitions are tagged `[Integration]` (Petitions also `[Guarded]`) on **every single item**, meaning the doc's own author already flagged these as needing a real external system, not just a missing table — building a thin manual stand-in wouldn't fulfill that and risks the same legal-automation overreach as Compliance for Petitions specifically. **§8 Survey/script effectiveness was different: genuinely just incomplete.** Items 1-2 ("What questions are in the current survey?" / "What is the current door script?") are tagged `[Now]` for a real reason — Lynx generates canvassing scripts on demand via AI (`canvassing_script` purpose) but never stored an official CURRENT one anywhere. Fixed: new migration `0034_campaign_scripts.sql` (one table, `kind: 'door_script' | 'survey_question'`, `content`, `sort_order`, `active` — retire via `active=false`, no deletes), gated on the existing `turf.view`/`turf.manage` permissions. New pure module `src/features/scripts/scriptMath.ts` (+ 5 tests): `currentDoorScript` / `currentSurveyQuestions`. New hooks `src/features/scripts/useScripts.ts`. Two new advisor answers (`answerCurrentDoorScript`, `answerCurrentSurveyQuestions`) folded into the Advisor Insights card, plus a small "Set the current door script / survey" management form added directly to that card (textarea + question input, gated on `turf.manage`). Same hand-patch caveat as Round 5 applies again — `campaign_scripts` was added to `src/lib/supabase/types.ts` by hand, not regenerated (Docker still not running). `npm run typecheck` / `npm run test` (408/408, up from 399, +9 new) / `npm run build` all pass. Not verified live. Not committed. **All remaining sections (§13, §14-15, §17, §18) stay deferred for the reasons above — the "problem" with them isn't something more coding fixes.**

**Prior change: Staffing (§9) and Logistics (§11) fully built — new migration, real UI, advisor Q&A.** Owner chose "build both now" at the decision point below. New migration `0033_staffing_logistics.sql`: two tables, `shifts` (project_id, profile_id, shift_date, status: scheduled/worked/off/pending_swap, team_name) and `hotel_bookings` (project_id, hotel_name, team_name, check_in, check_out, room_count, nightly_rate_cents) — both RLS'd and granted per the usual pattern (no DELETE grants), reusing the existing `hr.view`/`hr.manage` permissions rather than inventing new ones (shifts has an extra self-visibility clause: `profile_id = auth.uid()`, so a canvasser sees their own assignment even without `hr.view`, same pattern as `dashboard_layouts`). **Important caveat: local Supabase/Docker was not running this session, so `src/lib/supabase/types.ts` could not be regenerated the normal way** (`npx supabase gen types...`) — instead the `shifts`/`hotel_bookings` `Row`/`Insert`/`Update`/`Relationships` entries were hand-added to that file, matching the migration's columns/nullability/FKs as closely as possible by hand. `npm run typecheck` passes clean against this hand-patched file, but **the very first thing to do once Docker is available is `npm run db:reset` + regenerate types for real and diff against what's there now** — if the real codegen output differs from the hand-patched version in any way, that's the source of truth, not this file. New pure module `src/features/staffing/staffingMath.ts` (+ tests, 7 tests): `summarizeShiftsForDate` (real per-day headcount by status + team breakdown) and `summarizeLogistics` (real nights-between-dates x room x staff-entered nightly rate = an honest cost *estimate*, never a promise — no tax/fees modeled). New hooks file `src/features/staffing/useStaffing.ts` (`useShifts`, `useCreateShift`, `useUpdateShiftStatus`, `useHotelBookings`, `useCreateHotelBooking`). Two new advisor answers in `advisorInsights.ts` (`answerTodaysStaffing`, `answerLodgingCost`), wired into the existing Advisor Insights card in `AiCenterTab.tsx` — RLS already degrades gracefully for a viewer without `hr.view` (empty/self-only), so no extra gating needed there. UI home: `TeamTab.tsx` (already org+now-project-scoped — `ProjectDetailsPage.tsx` was updated to pass `projectId` alongside `orgId`) gained two new sections, `StaffingSection` and `LogisticsSection`, each self-gated on `hr.view` (visible) / `hr.manage` (create/edit controls) — a roster table + add-shift form, and a bookings list + add-booking form. `npm run typecheck` / `npm run test` (399/399, up from 388, +11 new) / `npm run build` all pass. **Not verified live in the browser** — same Docker blocker as every round this session, and now doubly important to check here specifically (creating a shift/booking through the real UI against the real hand-patched-then-regenerated schema is the real test). Not committed.

**Prior change: two more question-bank sections closed out — §10 coaching/safety and §23 multi-project rollup — plus a decision point on the rest.** Owner said "keep going, finish what you didn't build" after the round below. Added: `answerCanvasserWellbeing` in `advisorInsights.ts` (reuses `canvasserFatigue.ts` + `canvasserSilence.ts`, already-existing Turf Briefing detectors — folded into the Advisor Insights card, no new data). Separately, §23 (multi-project rollup) got its own new home since `AiCenterTab` is per-project: `src/features/dashboard/projectRollup.ts` (+ tests) is a pure, explainable "which project needs attention" ranking (real unmapped-voter backlog, offset by real dollars raised — never a prediction), wired into a new `ProjectAttentionRollup` component in `src/features/dashboard/widgets.tsx`, rendered on `HomePage.tsx` next to the existing `OrgDashboard` — only shows with 2+ projects and `projects.manage` permission. `npm run typecheck` / `npm run test` (388/388, up from 382, +6 new) / `npm run build` all pass. **Stopped here to ask the owner before going further**, because everything still unbuilt needs either an irreversible new migration + new UI-placement decisions (Staffing §9, Logistics §11 — note `hr.view`/`hr.manage` permissions already exist in `0003_roles.sql`, already granted to a seeded `HR` role template, so Staffing is clearly an anticipated-but-never-built domain, not scope invention) or is blocked outright (Compliance §13 stays excluded per invariant #6; SMS/email §14-15 needs a real third-party provider + credentials only the owner can supply; Survey §8 needs a structural redesign of how canvass responses are captured; Events §17 / Petitions §18 are `[Integration]` in the owner's own doc, petitions specifically carrying real legal-validity stakes). Not committed.

**Prior change: AI Campaign Advisor expanded into 6 more question-bank sections — canvassing performance, turf/geography rankings, fundraising pace, social performance, cross-channel attribution, and scenario planning — reusing existing Turf Briefing math rather than duplicating it.** Owner directive: build out everything from the question-bank doc that hadn't been touched yet, don't stop, and produce a handoff/artifact before running out of budget (see `LYNX_AI_ADVISOR_HANDOFF.md` in the scratchpad / the published artifact for the full session summary — this entry is the terse version). Two new files do the fetching/computing: `src/features/ai/advisorData.ts` (Supabase-importing hook: `useAdvisorSocialPosts`) and `src/features/ai/advisorInsights.ts` (pure, 14 tests via `advisorInsights.test.ts`) — the latter mostly *reuses* existing pure math (`canvasserStats.ts`'s leaderboard, `territoryDifficulty.ts`'s ranking, `visitHistory.ts`'s best-time-to-knock and persuasion-drift detectors) rather than recomputing it, reformatted as advisor Q&A. New: `computeFundraisingPace` (real 7-day-vs-prior-7-day trend from `donations.donated_at`, no new migration), `computeSocialPerformance` (real impressions/engagement from `social_posts`, migration 0013), `computeDoorstepAttribution` (real split of `donations.voter_id` null-vs-not, migration 0032), `scenarioAdjustPace`/`scenarioAddWorkers` (generic pace "what if" calculators), and two alerts (`describeFundraisingMomentumAlert`, `describeDataFreshnessAlert`) that only fire on a genuinely large swing/staleness — not noise on a normal week. A third new file, `src/features/ai/roleQuickPrompts.ts` (4 tests), adds permission-tailored quick-question chips above the free-text Ask box — driven by which tool categories the viewer's permissions unlock (no hook exposes a literal role name to this component, so this is the honest approximation). One pre-existing file got a 2-line additive edit: `src/features/rbac/toolRegistry.ts` gained a new `advisor_insights` tool entry (its own documented "add one entry, no other plumbing" recipe) so the new "Advisor Insights" card is RBAC-curated like every other AI tool. Everything from the two prior rounds (`cannedAnswers.ts`, `DATA_SYSTEM` prompt, `conversationContext.ts`) is untouched. `npm run typecheck` / `npm run test` (382/382, up from 347, +35 new) / `npm run build` all pass. **Not verified live — Docker/local Supabase still not running this session.** Not committed. **What was deliberately NOT built, and why** — full detail in the handoff artifact: compliance-guarded questions (§13, permanent exclusion per CLAUDE.md invariant #6, not a TODO); SMS/email/push/calling analytics and messaging-strategy analytics (§14–15, Lynx's "comms" tab is internal broadcasts + drafting tools only, no outbound voter-facing send/delivery tracking exists); staffing/scheduling and logistics/travel (§9, §11, no data model); survey/script effectiveness (§8, no structured survey-response table, only free-text notes); events and petitions (§17–18, explicitly `[Integration]` in the owner's own doc, no schema exists, would mean building new feature areas from scratch); multi-project/org-wide rollups (§23, architectural mismatch — `AiCenterTab` is rendered per-project with one `project` prop, a real rollup needs a new org-level surface, not an additive change to this one).

**Second-latest change: AI Campaign Advisor's "Ask your data" box is now a real multi-turn conversation, not single-shot Q&A.** Owner directive: stick to the AI Campaign Advisor only until told otherwise, and don't touch the canned bank / `DATA_SYSTEM` prompt from the previous change (below) — everything here is purely additive, in a new pure module `src/features/ai/conversationContext.ts` (+ `conversationContext.test.ts`, 11 tests), wired into `AiCenterTab.tsx` only. Four upgrades: (1) **conversation memory** — `buildFollowUpInstructions` recaps the last 2 turns into the next question client-side (never touches the edge function), so "Why does that matter?" resolves against what was just asked; (2) **follow-up chips** — `FOLLOW_UP_PROMPTS` (Why does that matter? / Show your math. / What data are you missing?) render under every answer, deliberately limited to questions answerable from the SAME static snapshot (no fabricated "compared to last week" — there's no time-series data to back it); (3) a **scope line** ("Based on 100 voters and 10 donations in this project") from `buildScopeLine`, shown once per conversation; (4) an honest **data-confidence heuristic** from `assessConfidence` — sample-size only (low/moderate/high by voter count), explicitly NOT a per-question statistical confidence score since the app has no way to compute that from an aggregate snapshot. `ask.data`'s old single-answer render was replaced by a scrollable `thread` array so history is visible; the 9 canned "Quick answers" cards and their "Ask AI to elaborate" links are unchanged and still call into this same upgraded `runAsk`. `npm run typecheck` / `npm run test` (347/347, up from 336, +11 new) / `npm run build` all pass. **Not verified live in the browser — local Supabase (Docker) still not running this session**, same blocker as the entry below. Not committed.

**Prior change: AI Campaign Advisor's canned bank grew from 4 to 9 questions, and its free-text system prompt now follows an explicit answer contract.** The owner supplied a large "AI Campaign Advisor" question-bank/capability-map document (Describe/Diagnose/Forecast/Recommend, tiered Now/Derived/Integration/Guarded) and asked for the existing AI Campaign Advisor feature to be evolved against it. Scope taken: only the genuinely **[Now]**-tier questions answerable from data the app already computes, so nothing here fabricates a capability. `cannedAnswers.ts` gained 5 new pure functions — `answerVoterUniverse` (voter count + mapped %), `answerContactsLogged` (voters with a canvass note, the closest proxy to "meaningful contacts" without a contact-attempt table), `answerUnassignedTerritories`, `answerFundraisingTotal`, `answerAverageDonation` — all built from the same `TurfSnapshot`/`FundraisingSnapshot` already in `AiCenterTab.tsx`, no new queries or migrations. `buildCannedAnswers` now returns 9 in a fixed order (voter universe, contacts logged, ballots outstanding, unassigned territories, top city, geocoding backlog, fundraising total, average donation, compliance progress). Separately, `DATA_SYSTEM` in `supabase/functions/ai-assist/index.ts` (the `data_qa` purpose, i.e. the free-text "Ask your data" box) was rewritten to require the answer shape from the owner's doc — direct answer, then supporting numbers, then interpretation — and to explicitly distinguish a snapshot-supplied number from anything calculated from it; recommendation/confidence/drill-down were deliberately left out of the contract because they'd need real historical-trend data this snapshot doesn't carry, and the prompt would otherwise be asking the model to fake maturity it doesn't have. `npm run typecheck` / `npm run test` (336/336, up from 324, +12 new tests in `cannedAnswers.test.ts`) / `npm run build` all pass. **Not verified live in the browser this pass — local Supabase (Docker) was not running in this session, so sign-in itself failed (`TypeError: Failed to fetch` from `AuthProvider.tsx`), the same blocker, not a regression.** Not committed, same as everything else in this file.

**Owner directive, still current — read before doing anything else:** the owner does **not** want a Fundraising-tab focus right now. The Fundraising-tab features already built this session (Bundler Network Detector, High-Dollar Event Planner, Contribution Limit Guardian — see §2 below) stay in the codebase as-is; **do not build anything new in the Fundraising tab.** All other new feature work goes into **Turf Briefing and its OpenStreetMap/MapLibre map only**, until told otherwise — the Ask Your Data change above is a targeted fix inside the existing AI Center, not a new feature area, so it doesn't conflict with that directive.

**Map-Integrated AI round verification is now complete.** All three features confirmed live in the browser against real local Supabase data (see §1a and §4 for full detail):
- Click-to-Ask Door Popup — clicking a pin switched Door Prep to that voter with "(selected on map)" + a working "Clear map selection" button that reverted to the algorithm's auto-pick.
- Draw-an-Area AI Briefing — a hand-drawn ring around 3 clustered synthetic voters correctly excluded a 4th, distant one; "Get area briefing" reached the `ai-assist` edge function and surfaced the expected 503 (no `ANTHROPIC_API_KEY` set locally) as an in-UI error, proving the whole pipeline (`findVotersInRing` → `buildBriefingSnapshot` → `map_area_briefing`) is wired correctly.
- Live Hotspot Caller — toggling Heatmap to "Density" produced the orange card "Hottest real 'density' pocket right now: 3 voters near 100 Cluster St, 102 Cluster St, 104 Cluster St" — real street addresses, above the daylight card.

`npm run typecheck` / `npm run test` (313/313) / `npm run build` all still pass clean. `CLAUDE.md` already documents this round in full (79 AI purposes, verified by direct read — it was ahead of what this file previously claimed). The 4 synthetic verification voters have been deleted from the local dev DB, and the temporary `window.__debugMap = map` debug line (reintroduced this session, per the note this file itself left) has been reverted — confirmed absent via `grep` and a clean `typecheck`.

**One environment note for whoever resumes next:** this sandbox's Browser pane sometimes runs with `document.hidden === true` (not actually composited on-screen), which stalls MapLibre's `requestAnimationFrame`-driven render/style-load pipeline indefinitely — `map.loaded()`/`isStyleLoaded()` never flip true, and `computer{action:"screenshot"}` fails with "the Browser pane is not displayed." This is a viewer-visibility issue, not an app bug (network fetches for the style/sprite/tiles all succeed on request). Workaround used this session: reintroduce `window.__debugMap = map` (same pattern the prior session used), then instead of pixel-clicking rendered pins, call the map's own registered event listeners directly and synchronously — `map._delegatedListeners.click[0].listener({ features: [{ properties: { voterId } }] })` for the voters-layer pin handler, and `map._listeners.click[0]({ lngLat: { lng, lat } })` for the generic draw-ring handler in `TurfTab.tsx`. This exercises the real app code (React state, real query params sent to the edge function) while only bypassing MapLibre's own internal tile rendering, which is a well-tested third-party library, not app logic. Always revert the debug global before finishing.

**Next: this session has accumulated 17+ new features across two domains without a single commit.** Per the "then stop and ask the owner" step from the prior handoff, the next action is to ask the owner whether to keep building more Turf-Briefing/map features or pause to commit — not to unilaterally decide either way.

---

# What's built this session

## 1. Turf Briefing — real-time canvassing intelligence (`TurfBriefing.tsx`, ~1,550 lines)

The centerpiece of this session: a new section of the Turf tab, built in four rounds, backed by three new migrations (`0029_turf_briefing.sql`, `0030_canvass_visits.sql`, `0031_turf_preferences.sql`).

**Round 1 — foundation**
- Live shift heatmap on the existing MapLibre map (density / persuadability / fundraising-signal / staleness modes)
- Party and persuadability pin-color modes inferred from real voter-file columns and canvass notes — never fabricated
- Live stat bar, one-click route rebalancing
- `turf_briefing` purpose: a grounded pre-shift captain briefing

**Round 2 — history-driven intelligence**, unlocked by `canvass_visits` (0030), an append-only log of every real door contact instead of an overwrite:
- Best Time to Knock (`visitHistory.ts` — real contact-success rate by hour)
- Persuasion Drift Alerts (a door whose lean changed between real visits)
- Household Rollup (`households.ts` — voters at the identical address collapse into one physical door)
- Daylight-Aware Shift Clock (`daylight.ts` — zero-dependency sunrise/sunset math)
- Live Objection Assistant (`door_objection_assist` — real-time at-the-door coaching)

**Round 3 — per-user customization**, via `turf_briefing_preferences` (0031, same RLS pattern as `dashboard_layouts`):
- Customize panel — every threshold staff-adjustable and persisted
- `quick_insight` layered onto the pure-math stats
- 5 more purposes: `door_script_personalize`, `door_explainer`, `door_language_prep`, `shift_debrief`, `territory_difficulty_briefing`

**Round 4 — canvasser coordination**, reusing `canvass_visits.canvasser_id`:
- Canvasser Leaderboard (`canvasserStats.ts`)
- Cross-Canvasser Overlap Guard (`overlapGuard.ts` — flags a household two canvassers actually visited within a lookback window; informational, never blocks a route)
- Revisit Queue (`revisitQueue.ts` — real repeated no-answer doors, ranked by attempt count)
- `revisit_strategy` — honest verdict on the single most-attempted door, allowed to say it's not worth another try

All Turf Briefing purposes sit under one `turf_briefing` tool-registry entry, gated on `turf.view`.

## 1a. Map-Integrated AI round (this session, most recent — verification in progress, see "Resume here" above)

Owner directive: stop building in the Fundraising tab, focus only on Turf Briefing **and its map**. Grounded first by actually reading `TurfTab.tsx`'s MapLibre code — clicking a pin did nothing at all before this round (only "Draw territory" mode responded to map clicks). Three features, all genuinely new map interactions, not AI bolted onto something that already existed:

- **Click-to-Ask Door Popup** — clicking any pin now sets `mapSelectedVoterId` (new state in `TurfTab.tsx`, lifted from a new `voters-layer` click handler that reads a `voterId` property now pushed onto every GeoJSON point — individual mode uses the real voter id, household mode uses the first real member's id). Passed down to `TurfBriefing.tsx` as props (`mapSelectedVoterId`, `onClearMapSelection`); `topDoor` (what Door Prep operates on) now prefers the map-selected voter over its own auto-picked top-priority door, with a "(selected on map)" label and a "Clear map selection" button. Reuses the **existing** `door_script_personalize`/`door_explainer` purposes unchanged — zero new AI purpose, just a new way to reach them (any real door, not only the algorithm's top pick).
- **Draw-an-Area AI Briefing** — a new "Ask about area" button next to "Draw territory," reusing the exact same click-to-add-vertex drawing mechanics (refactored `drawing: boolean` into `drawMode: 'off' | 'territory' | 'ask'` so both modes share one ref-guarded map click handler). Finishing in "ask" mode runs `findVotersInRing` (`areaSelect.ts`, new pure-math file reusing `@turf/turf`'s `booleanPointInPolygon` — the same primitive `useCreateTerritory` already uses, just without persisting anything) to find which real voters fall inside the hand-drawn shape, builds a real aggregate snapshot via the **existing** `buildBriefingSnapshot` (just scoped to the filtered subset instead of the whole project), and sends it to a **new** AI purpose, `map_area_briefing`, distinct from `turf_briefing` (whole filtered set) and `territory_difficulty_briefing` (named, saved territories only) because this is any arbitrary shape drawn by eye, never saved.
- **Live Hotspot Caller** — turns the *existing* heatmap from a color overlay into words. New pure-math module `hotspot.ts` grid-bins real mapped voters (~300m cells) and reuses `turfBriefingMath.ts`'s own `heatmapWeight` function directly (so the words always agree with what's actually drawn — zero duplicated weighting logic), finds the single densest real cell for whichever heatmap mode is active, and surfaces its real street addresses. `quick_insight`-decorated (like Best Time to Knock, Doorstep Leaderboard), not a new dedicated purpose. Only computed at all when a heatmap mode is actually on.

**+1 new AI purpose this round** (`map_area_briefing`) — pending docs update, expected total **79 AI purposes** in the app (see "Resume here" §6 above).

New files: `src/features/turf/areaSelect.ts` (+ test, 5 tests), `src/features/turf/hotspot.ts` (+ test, 6 tests). Modified: `TurfTab.tsx` (click handler, drawMode refactor, new GeoJSON properties, new "Ask about area" UI block), `TurfBriefing.tsx` (new props, `mapSelectedDoor`/`hotspot` computation, new JSX cards), `supabase/functions/ai-assist/index.ts` + `src/lib/ai/useAiAssist.ts` (new purpose).

**A real bug was caught and fixed by actually running the tests** (not just eyeballing the code): the first draft of `hotspot.test.ts` used coincidentally grid-aligned lat/lng test fixtures (e.g. `-75.222` divides evenly by the 0.003° cell size), which put two "obviously close together" points in different bins purely by floating-point coincidence — a real lesson in why round test numbers can be misleading for any grid-binning algorithm, fixed by using more realistic non-aligned coordinates.

## 2. Fundraising Intelligence — revenue layered onto Turf Briefing's signals (2 rounds, 10 purposes, no shared UI file)

Unlocked by one structural addition: `donations.voter_id` (migration `0032_donation_voter_link.sql`, nullable — null for every online/mail/event gift, which is most of them). Set only via the new **Record gift** action in `DoorstepDonations.tsx` (extends `useRecordDonation` with an optional `voterId`).

**Round 1:**
- `momentum_ask_script` (`momentumAsk.ts`) — a door that just turned persuadable per a real Persuasion Drift alert and has never been asked
- `household_cascade_ask` (`householdCascade.ts`) — one household member gave, the rest are a warm cross-sell (lives in `DoorstepDonations.tsx`)
- `peak_ask_briefing` (`peakAskWindow.ts`) — real $/hour from linked doorstep gifts only, excluding online/mail
- `territory_roi_briefing` (`territoryFundraisingRoi.ts`) — real $ raised per door knocked, by territory
- `persistence_ask_script` (`persistenceAsk.ts`) — a door reached only after real repeated no-answer attempts is a distinct reciprocity moment

**Round 2:**
- `golden_hour_ask_plan` (`goldenHourPush.ts`) — once real daylight is running low, the highest-$ warm doors still reachable outrank one more unscored knock
- `ask_coverage_alert` (`askCoverageGap.ts`) — a household multiple canvassers visited that no one has actually asked — a coordination failure, not a data gap
- `canvasser_ask_coaching` (`canvasserAskCoach.ts`) — cross-references the door-knocking leaderboard with the $ leaderboard by profile id; ranked lowest-ask-rate-first, 3-contact minimum before it coaches anyone
- `election_countdown_ask` (`electionCountdownAsk.ts`) — the fundraising analog of the GOTV Sprint Planner; real staff-entered election date drives urgency on never-given warm doors
- `doorstep_recurring_ask` (`doorstepRecurringUpgrade.ts`) — a doorstep-linked donor with a currently-supportive real lean is a monthly-upgrade candidate on the next visit (lives in `DoorstepDonations.tsx`)

All ten purposes fold under the existing `turf_briefing` tool-registry entry.

**Round 3 — three more tools, chosen to complement Turf Briefing along axes nothing else covered:**
- `priority_door_briefing` (`priorityDoor.ts`) — the turnout-focused analog of Golden Hour Push. Synthesizes real persuadability + ballot status, the Revisit Queue, and the real election countdown into one ranked "hit these doors next" list. Deliberately excludes fundraising warmth — that axis already has Golden Hour Push.
- `canvasser_checkin_prompt` (`canvasserFatigue.ts`) — detects a real decline in one canvasser's own contact rate between the first and second half of today's shift, drafts a supportive check-in message. Framed as a wellbeing nudge, never a performance write-up.
- **Volunteer Cadence Detector** (`canvasserCadence.ts`, wired into `VolunteerPipeline.tsx`, no new AI purpose) — `canvass_visits` now has the real per-canvasser weekly history needed to auto-detect a lapsing or newly-accelerating volunteer, one-click-filling the existing `volunteer_pipeline` purpose's situation field.

**Round 4 — staffing balance and canvasser safety, not per-door tactics:**
- `territory_staffing_briefing` (`territoryStaffing.ts`) — real remaining-door load per territory against how many distinct real canvassers worked it in the last 7 days; recommends reallocating only above a 3x workload-ratio gap and only from a territory with a real canvasser to spare.
- `canvasser_silence_checkin` (`canvasserSilence.ts`) — flags a canvasser with a real established presence today (2+ visits) who has then logged nothing for 90+ minutes. Distinct from Pace Check-in's declining-rate-while-still-active signal.

**Round 5 — donation-conversion-focused** (owner framed this as "get 100x more donations than competitors"; treated as directional enthusiasm, not a literal spec — confirmed with the owner before building; nothing fabricates a metric/urgency claim):
- `neighborhood_proof_ask` (`neighborhoodProof.ts`) — real doorstep gifts grouped by street name surface how many of a warm door's actual neighbors have already given. Only shown when the real count is > 0.
- `donation_objection_handler` — drafting-only. A canvasser types exactly what a voter said when declining/stalling on a donation ask; distinct from `door_objection_assist` (general political pushback); drafts an optional same-day text-to-give follow-up only for a "no cash"/"need to think" stall.
- `ask_rehearsal_prep` (`askRehearsal.ts`) — a confidence-building tool BEFORE a canvasser starts asking, distinct from `canvasser_ask_coaching` (coaches after the fact). Built from an aggregate-only snapshot of today's warm-door count and common reasons.

**Round 6 — owner feedback that Round 5 was too similar-in-kind (script-generators); these three change mechanics instead:**
- **Money Route Optimizer** (`moneyRoute.ts`) — an "Optimize for $" button next to Rebalance Now, reorders today's remaining doors to walk every real warm door first (reusing `optimizeWalkOrder`'s nearest-neighbor + 2-opt on two subsets). Zero new AI purpose.
- **Live Team Fundraising Goal Tracker** (`teamGoalTracker.ts`) — a session-local dollar-goal input, live progress bar from today's real doorstep gifts, honest pace-based "on pace for ~$X by sunset" projection using real remaining daylight. `quick_insight`-decorated, not a new purpose.
- `doorstep_referral_ask` — fires automatically the instant a real gift is recorded (`saveGift`'s `onSuccess` in `DoorstepDonations.tsx`), asking for a referral while the donor's "yes" is warm.

**Major-donor round** (owner directive: a campaign chasing millions lives at the major-donor/bundler/event level; three features in `src/features/fundraising/` — **per the current owner directive above, this is the last Fundraising-tab work; do not add more here**):
- **Bundler Network Detector** (`bundlerNetworkMath.ts` + `BundlerNetwork.tsx`) — clusters real donors sharing a real employer who have ALL actually given, surfaces the cluster's highest real giver as the one to ask to formally cultivate coworkers. `bundlerNetworkMath.ts` is named with a `Math` suffix (not `bundlerNetwork.ts`) — **see the filesystem gotcha below, this bit twice this session.**
- **High-Dollar Event Planner** (`eventPlanner.ts` + `HighDollarEventPlanner.tsx`) — ranks real donors by lifetime giving into an invite list anchored to each person's own real largest gift, computes an honest realistic dollar range, honest gap assessment if a target exceeds it.
- **Contribution Limit Guardian** (`contributionLimitMath.ts` + `ContributionLimitGuardian.tsx`) — deliberately ZERO AI, same rule as Compliance. Flags real donors/employer clusters approaching a STAFF-ENTERED threshold, same "not legal advice" banner pattern as `ComplianceTab.tsx`.

**⚠️ Filesystem gotcha (hit twice this session, will hit again if forgotten):** this dev box's filesystem is case-insensitive. A pure-math file and its component **cannot** share a name differing only in case — `bundlerNetwork.ts` next to `BundlerNetwork.tsx` and `contributionLimitGuardian.ts` next to `ContributionLimitGuardian.tsx` both broke `tsc` with a "differs only in casing" error. Fix/pattern going forward: **always** suffix the pure-math file with `Math` (`turfBriefingMath.ts`, `bundlerNetworkMath.ts`, `contributionLimitMath.ts`) whenever the natural pure-math name would otherwise exactly match a component name.

**79 AI purposes total** in the app now, pending the docs update noted in "Resume here."

## 3. Supporting infrastructure

- **RLS tests**: `npm run test:rls` (pgTAP via `supabase test db --local`) — coverage for `canvass_visits` and `turf_briefing_preferences` (`supabase/tests/*_rls_test.sql`), each a self-contained rolled-back transaction
- **Component testing**: vitest runs under `jsdom` (`vite.config.ts`'s `test` block, `vitest/config`'s `defineConfig`), `@testing-library/react` + `jest-dom` + `user-event` added as devDependencies. First component test: `TurfBriefing.test.tsx`
- `route.ts`'s `pickField()` helper exported for `turfBriefingMath.ts` reuse
- `useFundraising.ts`: `Donation.voter_id` field + `useRecordDonation`'s optional `voterId` param
- `BallotChase.tsx`: status/notes mutations pass `current: voter` alongside the update

---

## 4. Verified this session

✅ `npm run typecheck` clean (including after the Map-Integrated AI round)
✅ `npm run test` **313/313 passing** across 47 test files (218 at the start of this session's docs pass → +21 Round 3 → +16 Round 4 → +11 Round 5 → +12 Round 6 → +24 major-donor round → +11 Map-Integrated AI round)
✅ `npm run build` clean
✅ Local Docker Supabase used for all verification, live browser passes with synthetic data for every round **including** the Map-Integrated AI round (verified 2026-07-26 — see "Resume here")

### Note on AI testing
No real model responses tested locally (no `ANTHROPIC_API_KEY` set in dev). All purposes reach the edge function correctly; expected "AI is not configured" 503 locally.

---

## 5. Known gaps

- **No component/e2e coverage beyond `TurfBriefing.test.tsx`** — everything else new is pure-logic vitest only
- **RLS pgTAP coverage** exists for only 2 of the many RLS-protected tables — see `docs/TODO.md` #8
- **Household rollup dedup** is exact-address-string only, deliberately conservative (no fuzzy matching)
- `lynx-tools-features.{csv,json,md}` (a stale, untracked Jul 22 export, not read by the app) were deleted as housekeeping
- Compliance domain remains deliberately AI-free (invariant #6) — none of this session's AI additions touch it

---

## 6. What wasn't done

- Nothing has been **committed** or **pushed** to GitHub
- Nothing has been **deployed** to production Supabase or Vercel
- No **real AI model responses** tested (edge function verified, but no `ANTHROPIC_API_KEY` set locally)

---

## 7. Next steps

**Current focus, per the owner:** Turf Briefing and its map ONLY — no more Fundraising-tab work. Map-Integrated AI round verification is complete (see "Resume here" at the top) — the immediate next action is to ask the owner directly, not to decide unilaterally.

1. **Ask the owner whether to keep building more Turf-Briefing/map features or pause to commit.** This session has built **17+ new features** across two domains (Turf Briefing/map + Fundraising) with zero commits. If pausing to commit, suggest splitting by round rather than one giant commit.
2. Once ready to ship: set up `ANTHROPIC_API_KEY` in production, run `npx -y supabase@latest db push` for migrations `0028`–`0032`, test live with real model generations, then push to `feat/lynx-platform` and open a PR against `main` (owner has asked to wait on pushing — confirm before doing so).

---

## 8. Contacts & context

- **Owner scope directive (current):** Turf Briefing + its map only, no new Fundraising-tab features, until told otherwise. Existing Fundraising-tab code from this session stays, just isn't being added to.
- **Live app users** will see this work the moment it merges to `main` and Vercel redeploys
- **RLS is the enforcement layer** — all role-based filtering (`toolRegistry.ts`, UI gates) is presentation only; database RLS policies are the real gate
- **All aggregate data only** — no raw voter rows or personal info sent to AI
- **Compliance stays AI-free** (invariant #6) — a hard legal-risk line, not a style choice
- **Filesystem gotcha** — see §2 above, always suffix a colliding pure-math file with `Math`

---

**Prepared by:** Claude Code · **Date:** 2026-07-26 · **Status:** Map-Integrated AI round code-complete AND verified (typecheck/test/build clean, all three features confirmed live against real local data) — waiting on the owner to say whether to keep building or pause to commit, see "Resume here"
